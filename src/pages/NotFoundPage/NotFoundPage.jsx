import css from '../NotFoundPage/notFoundPage.module.css';

const NotFoundPage = () => {
  return <p className={css.message}>Not found page. Please click Home or Movies.</p>;
};

export default NotFoundPage;